VITE_SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Backend API base URL (set this in Vercel project environment for production)
# Example for Render deployment:
VITE_BACKEND_URL="https://dayclap-backend-api.onrender.com" # Updated to production URL example

# Flask Backend Environment Variables (for local dev only, if you run backend locally)
FLASK_SECRET_KEY="YOUR_LOCAL_FLASK_SECRET_KEY"
DAILY_EMAIL_API_KEY="YOUR_LOCAL_DAILY_EMAIL_API_KEY"

# Maileroo Configuration (for local dev only, if you run backend locally)
MAILEROO_API_KEY="YOUR_LOCAL_MAILEROO_SENDING_KEY"
MAILEROO_API_ENDPOINT="https://smtp.maileroo.com/api/v2/send" # CORRECTED: Added /send
MAIL_DEFAULT_SENDER="no-reply@team.dayclap.com"

# VAPID Public Key for Push Notifications (Frontend uses this to subscribe)
# This should match the VAPID_PUBLIC_KEY set in your backend's production environment.
VITE_VAPID_PUBLIC_KEY="YOUR_VAPID_PUBLIC_KEY_FOR_FRONTEND"

# Admin emails allowed on backend /api/admin/* (set in backend env; provided here for reference)
# Example in backend env: ADMIN_EMAILS="admin@example.com,admin@dayclap.com"
ADMIN_EMAILS="admin@example.com"
